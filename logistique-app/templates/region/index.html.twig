{% extends 'base.html.twig' %}

{% block title %}Liste des Régions{% endblock %}

{% block body %}
<div class="page-body">
    <div class="container-xl">
        <div class="row row-cards">
            <div class="col-12">
                <div class="card card-table">
                    <div class="card-header">
                        <h3 class="card-title">Régions</h3>
                        <div class="ms-auto">
                            <a href="{{ path('app_region_new') }}" class="btn btn-primary d-none d-sm-inline-block">
                                <i class="fas fa-plus me-2"></i> Nouvelle Région
                            </a>
                        </div>
                    </div>
                    <div class="table-responsive">
                        <table id="region-table" class="table card-table table-vcenter text-nowrap datatable">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Nom</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for region in regions %}
                                <tr>
                                    <td>{{ region.id }}</td>
                                    <td>{{ region.nom }}</td>
                                    <td>
                                        <a href="{{ path('app_region_show', {'id': region.id}) }}" class="btn btn-icon btn-sm" title="Détails">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="{{ path('app_region_edit', {'id': region.id}) }}" class="btn btn-icon btn-sm" title="Modifier">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPER_ADMIN') %}
                                            <form method="post" action="{{ path('app_region_delete', {'id': region.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette région ?');" class="d-inline">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region.id) }}">
                                                <button type="submit" class="btn btn-icon btn-sm text-danger" title="Supprimer">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        {% endif %}
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="3">Aucune région trouvée.</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
    $(document).ready(function() {
        $('#region-table').DataTable({
            language: {
                url: '//cdn.datatables.net/plug-ins/2.0.8/i18n/fr-FR.json'
            }
        });
    });
</script>
{% endblock %}