{% extends 'base.html.twig' %}

{% block title %}Modifier une Région{% endblock %}

{% block body %}
<div class="page-body">
    <div class="container-xl">
        <div class="row row-cards">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Modifier la Région</h3>
                    </div>
                    <div class="card-body">
                        <form method="post" action="{{ path('app_region_edit', {'id': region.id}) }}">
                            <input type="hidden" name="_token" value="{{ csrf_token('edit_' ~ region.id) }}">
                            <div class="mb-3">
                                <label class="form-label" for="nom">Nom de la Région</label>
                                <input type="text" class="form-control" name="nom" id="nom" value="{{ region.nom }}" required>
                            </div>
                            <button type="submit" class="btn btn-primary">Mettre à jour</button>
                            <a href="{{ path('app_region_index') }}" class="btn btn-secondary">Annuler</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}