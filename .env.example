# Database Configuration
# Copy this file to .env and fill in your credentials.
# Production credentials are set via server environment variables.
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=logistiquenjs

# Session / Security
SESSION_INACTIVITY_TIMEOUT=3600
